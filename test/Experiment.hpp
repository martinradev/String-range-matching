#ifndef EXPERIMENT_HPP
#define EXPERIMENT_HPP

#include "SuffixArray.hpp"

#include <vector>

/*
	TODO
	load data
	check lcp array
	think of good way to select substring
*/

class Experiment {
	public:
	Experiment(const std::istream & data, const vector<size_t> & repetitionsCount); 
	private:
	
};

#endif
